import Example from 'docs/src/components/Example';
import Graph from 'examples/ports/ports';
import code from '!!raw-loader!examples/ports/ports.jsx';

# Ports

A common use-case is for connections to link to a designated area on a node boundary.

Such an area is called a _port_.

<Example
  code={code}
  output={<Graph />}
  path={'examples/ports/ports.jsx'}
  sourceOpen
/>
